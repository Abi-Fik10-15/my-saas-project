generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("postgresql://dbuser:dbpassword@postgres:5432/app_db?schema=public")
}

model User {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  password  String   // In a real app, this must be hashed (e.g., bcrypt)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}